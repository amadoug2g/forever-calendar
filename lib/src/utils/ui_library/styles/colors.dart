import 'package:flutter/material.dart';

const Color primaryColor = Color(0xFF383B53); // Space Cadet
const Color secondaryColor = Color(0xFF32213A); // Dark Purple
const Color surfaceColor = Color(0xFFBCD2EE); // Columbia Blue
const Color errorColor = Color(0xFFE57373);

const ColorScheme kColorScheme = ColorScheme.light(
  primary: primaryColor,
  secondary: secondaryColor,
  surface: surfaceColor,
  error: errorColor,
);
